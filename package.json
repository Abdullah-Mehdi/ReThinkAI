{
  "name": "rethink-ai-workspace",
  "version": "1.0.0",
  "description": "Educational AI Environment Quiz - Workspace Root",
  "scripts": {
    "build": "npm ci --cache /tmp/.npm --include=dev && npm run build --workspace=frontend",
    "start": "npm run start --workspace=frontend",
    "dev": "npm run dev --workspace=frontend",
    "lint": "npm run lint --workspace=frontend"
  },
  "workspaces": [
    "frontend"
  ],
  "private": true
}